
[package]
name = "nrf1"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "Thin facade over nrf-core — canonical ai-nrf1 encoder/decoder"

[dependencies]
nrf-core = { path = "../../impl/rust/nrf-core" }
