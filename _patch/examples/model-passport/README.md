# Example: Model Passport E2E

- `input/` : sample metadata JSON
- `scripts/run.sh` : runs canon → judge → bundle → verify
- `expected/` : expected artifacts (receipt.json, passport.tgz)

> Use a local SLM (Ollama/llama.cpp) for offline judgment.
