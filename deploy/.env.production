# ==========================================================================
# AI-NRF1 / UBL — Production Environment
#
# Copy to .env and fill in real values before running go-live.sh
# NEVER commit this file with real secrets.
# ==========================================================================

# --- Registry (BASE) ---
PORT=8791
RUST_LOG=registry=info,axum=info,tower_http=info,module_runner=info
ISSUER_DID=did:ubl:lab512
CDN_BASE=https://registry.ubl.agency

# --- Signing key (generate with: ubl keygen -o node) ---
# SIGNING_KEY_HEX=<64-hex-chars>

# --- Runtime attestation (sha256 of the release binary) ---
# BINARY_SHA256=<sha256>

# --- MODULES layer ---
STATE_DIR=$HOME/.ai-nrf1/state

# --- Webhook secrets ---
# WH_SEC=<your-webhook-hmac-secret>

# --- LLM provider ---
# ANTHROPIC_API_KEY=<your-anthropic-api-key>

# --- Relay ---
# PARTNER_RELAY_URL=https://relay.partner.net/ingest

# --- Cloudflare Tunnel ---
# TUNNEL_UUID=<uuid-from-cloudflared-tunnel-create>
# TUNNEL_HOSTNAME=registry.ubl.agency
