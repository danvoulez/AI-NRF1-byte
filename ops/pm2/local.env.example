# Local env for PM2 apps (DO NOT COMMIT secrets)

# Registry HTTP
PORT=8791
RUST_LOG=registry=info,axum=info

# Registry identity
ISSUER_DID=did:ubl:lab512-registry
CDN_BASE=https://registry.ubl.agency

# Stable signing key (32 bytes / 64 hex chars). If omitted, registry generates an ephemeral key (dev).
# SIGNING_KEY_HEX=

# Optional: set to the build hash you want recorded in runtime attestation
# BINARY_SHA256=

# Ledger (when compiled with module-ledger-ndjson feature; default is on)
LEDGER_DIR=/Users/danvoulez/registry-data/ledger
