[package]
name = "ledger-ndjson"
version = "0.1.0"
edition = "2021"

[dependencies]
ubl-storage = { path = "../../crates/ubl-storage" }
serde_json = "1"
async-trait = "0.1"
tokio = { version = "1", features = ["fs", "io-util", "sync"] }
